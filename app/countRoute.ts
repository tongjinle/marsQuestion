import * as express from "express";




let route = (app: express.Application) => {
    // login
    app.post('/count//analysis', (req: express.Request, res: express.Response) => {
        
    });

    // edit pwd
    app.post('/count/analysisSelf', (req: express.Request, res: express.Response) => {
        
    });


   

};


export let userRoute = route;